{
  "name": "backend",
  "version": "1.0.0",
  "main": "server.js",  // Changed from index.js since your file is server.js
  "scripts": {
    "start": "node server.js",  // Added start script needed for Vercel
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@sendgrid/mail": "^8.1.4",  // Added SendGrid since you're using it
    "body-parser": "^1.20.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",  // Added dotenv since you're using it
    "express": "^4.21.1"
  }
}
